document.addEventListener('DOMContentLoaded',()=>{const d=window.PW_DESTINATION;if(!d)return;document.title=`${d.name} | Pacific World Travel`;const root=document.getElementById('destination-root');if(!root)return;const photo=d.image?`style="background-image:url('${d.image}')"`:'';const photoClass=d.image?'':' no-photo';const pointHtml=d.points.map(x=>`<div class="ds-point"><strong>${x[0]}</strong><span>${x[1]}</span></div>`).join('');const programmeHtml=d.programmes.map((x,i)=>`<article class="ds-programme"><div class="ds-programme-image${photoClass}" ${d.image?`style="background-image:url('${i===1&&d.altImage?d.altImage:d.image}')"`:''}></div><div class="ds-programme-body"><small>${x[0]}</small><h3>${x[1]}</h3><p>${x[2]}</p><a href="contact.html">Tailor this programme →</a></div></article>`).join('');const shortlist=d.shortlist.map(x=>`<div class="ds-shortlist-item"><strong>${x[0]}</strong><p>${x[1]}</p></div>`).join('');root.innerHTML=`<section class="ds-hero${photoClass}" ${photo}><div class="ds-hero-inner"><div class="ds-crumbs"><a href="index.html">Home</a> / <a href="destinations.html">Destinations</a> / ${d.name}</div><h1>${d.name}</h1><div class="ds-hero-bottom"><p>${d.intro}</p><div class="ds-tags"><span>${d.region}</span><span>${d.tag}</span></div></div></div></section><section class="ds-overview"><div class="ds-overview-photo${photoClass}" ${photo}></div><div class="ds-overview-copy"><p class="ds-eyebrow">WHY ${d.name.toUpperCase()}</p><h2>${d.headline}</h2><p>${d.intro}</p><div class="ds-points">${pointHtml}</div></div></section><section class="ds-programmes"><div class="ds-heading"><div><p class="ds-eyebrow">${d.name.toUpperCase()} PROGRAMMES</p><h2>Useful starting points.</h2></div><p>Every programme is customisable around your travellers, meetings, group size, hotel level and preferred pace.</p></div><div class="ds-programme-grid">${programmeHtml}</div></section><section class="ds-essentials"><div class="ds-essential"><span>Best for</span><strong>${d.bestfor}</strong></div><div class="ds-essential"><span>Typical stay</span><strong>${d.typical}</strong></div><div class="ds-essential"><span>Access</span><strong>${d.access}</strong></div><div class="ds-essential"><span>Easy add-ons</span><strong>${d.add}</strong></div></section><section class="ds-section"><div class="ds-shortlist"><div><p class="ds-eyebrow">EXPERIENCE ${d.name.toUpperCase()}</p><h2>Make the time outside work count.</h2></div><div class="ds-shortlist-list">${shortlist}</div></div></section><section class="ds-faq"><div class="ds-faq-head"><p class="ds-eyebrow">${d.name.toUpperCase()} FAQS</p><h2>Frequently asked questions.</h2></div><div class="ds-faq-list"><details><summary>How long should we stay in ${d.name}?</summary><p>${d.typical} is a useful starting point, but we shape the timing around flights, meetings and the wider Vietnam itinerary.</p></details><details><summary>Can Pacific World manage the full programme?</summary><p>Yes. We can coordinate hotels, private transport, golf where relevant, dining, local experiences and on-the-ground support as one connected plan.</p></details><details><summary>Can this be combined with other destinations?</summary><p>Yes. ${d.add} can be connected into the same itinerary without changing travel partners.</p></details><details><summary>Are the sample programmes fixed?</summary><p>No. They are starting points only. Every Pacific World journey can be tailored around your travellers, goals and pace.</p></details></div></section><section class="ds-final"><div><h2>Planning ${d.name}?</h2><p>Tell us the dates, travellers and priorities. We’ll build the journey around them.</p></div><a href="contact.html">Start planning →</a></section>`;});